# Documentation infrastructure
This repo contains the reusable infrastructure for the documentation websites curated and deployed by ACCESS-NRI.

Examples of this reusable infrastructure are link-checker config file and reusable workflow, along with templates such as the `readthedocs_yaml_template`.
